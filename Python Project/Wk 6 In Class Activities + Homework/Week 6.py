
Dictionary

#Empty dictionary
d = {} or d = dict()

#Add
d[key] = value

#Remove
del d[key] || d.pop[key]

#Visit key, value, items
d.keys() || d.items() || d.values()

#Traverse all keys, value, items

for x in d.keys() || d.values()

x in d => reference to keys
d = {'a': 1, 'b': 2}